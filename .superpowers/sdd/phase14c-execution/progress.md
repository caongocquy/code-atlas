# SDD ledger — plan: Phase 14C execution

Preflight: current base branch HEAD a5bf0f8; accepted production ancestor d463d02; worktree branch codex/phase14c-framework-dynamic-edges.

Ruling: use current fix/cli-index-progress-ux HEAD a5bf0f8 as implementation parent because it contains the canonical Phase 14C spec commits; d463d02 remains the production baseline for regression comparison.

Preflight scan (2026-09-10):
- Shared 0.1 -> 0.2: framework.types supplies facts imports; 0.2 adds ObjectiveSyntax only; no field collision.
- Shared 0.2 -> 0.3: facts version constants owned by 0.2; framework version owned by 0.3; no duplicate bump.
- Shared 0.3 -> 0.4: IndexVersionDomains is extended before schema metadata; schemaVersion remains persistence-only.
- Shared 0.4 -> 0.5: schema tables precede AtlasStore APIs; 0.5 uses existing candidate lifecycle and does not switch active generation.
- Shared 0.5 -> 1.3/6.1: storage read API is generation-scoped; publisher and query projection must use the same active generation.
- 1.1 -> 1.2: config facts are materialized before detection; adapters receive snapshots only.
- 1.2 -> 1.3: gate output is FrameworkMaterialization; publisher requires completed same-generation framework state.
- 1.4 -> 1.5: invalidation plan and counters feed the shared fixture; no framework version bump in adapter tracks.
- Tracks 2/3/4/5 -> 6: each emits only shared typed adapter output; no framework track imports another adapter.
- 6.4 -> 6.5: verification evidence precedes closure report; closure cannot waive a new failure.
- Each task self-consistency: listed tests exercise the produced interface; listed Modify paths are audited repository paths; Create paths are new.
Ruling: if plan snippets conflict with the canonical spec, preserve the spec and record the smallest interface correction in the task ledger.

Fix round 1 report (2026-09-10):
- Reviewer gaps addressed: canonical six-element logical tuple validation with exact JSON serialization and sorted unique conditions; separate `FrameworkInfrastructureFailure`/`FrameworkDiagnosticOutcome` support for `framework_adapter_failed`; output-kind-discriminated coverage; bounded accepted provenance (nonempty unique IDs/refs, 32-item cap, bounded strings, repository-relative paths, validated source ranges).
- RED: `rtk proxy node --import tsx/esm --test test/phase14c-contracts.test.ts` -> 11 tests, 9 passed, 2 failed (malformed logical keys and unbounded provenance).
- GREEN: `rtk proxy node --import tsx/esm --test test/phase14c-contracts.test.ts` -> 11 passed, 0 failed, 0 skipped.
- GREEN: `rtk proxy node_modules/.bin/tsc --noEmit` -> exit 0.
- GREEN: `rtk proxy node_modules/.bin/tsc --noEmit --ignoreConfig --strict --types node --target ES2022 --module NodeNext --moduleResolution NodeNext --skipLibCheck test/phase14c-contracts.test.ts` -> exit 0.
- GREEN: targeted eslint for the four task files -> exit 0.
- No new ruling was required; no pipeline, storage, package, lockfile, or other test files were changed.

Fix round 2 report (2026-09-10):
- Reviewer gaps addressed: logical-key scope/router validation now rejects backslashes, dot segments, repeated separators, and other noncanonical relative paths without normalization; accepted provenance rejects unknown fields, enforces bounded serialized source ranges/evidence refs/provenance arrays, and preserves declared fields for valid payloads.
- RED: `rtk proxy node --import tsx/esm --test test/phase14c-contracts.test.ts` -> 12 tests, 10 passed, 2 failed (noncanonical separators and unknown payload fields).
- GREEN: `rtk proxy node --import tsx/esm --test test/phase14c-contracts.test.ts` -> 12 passed, 0 failed, 0 skipped.
- GREEN: `rtk proxy node_modules/.bin/tsc --noEmit` -> exit 0.
- GREEN: `rtk proxy node_modules/.bin/tsc --noEmit --ignoreConfig --strict --types node --target ES2022 --module NodeNext --moduleResolution NodeNext --skipLibCheck test/phase14c-contracts.test.ts` -> exit 0.
- GREEN: targeted eslint for the four Task 0.1 files and `rtk proxy git diff --check` -> exit 0.
- No pipeline, storage, package, lockfile, or out-of-scope production files were changed; the query projection file remains unchanged because prior review found it correct.

Fix round 2 report (2026-09-10):
- Reviewer gaps addressed: canonical entity scope/router paths now reject backslashes, dot segments, empty/repeated separators, and traversal without silently normalizing; accepted decoding rejects unknown fields across outputs, provenance, refs, ranges, and entity/subject payloads; serialized ranges, refs, evidence IDs/refs, and provenance are bounded while declared fields are preserved for valid payloads.
- RED: `rtk proxy node --import tsx/esm --test test/phase14c-contracts.test.ts` -> 12 tests, 10 passed, 2 failed (noncanonical separators and unknown payload fields).
- GREEN: `rtk proxy node --import tsx/esm --test test/phase14c-contracts.test.ts` -> 12 passed, 0 failed, 0 skipped.
- GREEN: `rtk proxy node_modules/.bin/tsc --noEmit` -> exit 0.
- GREEN: `rtk proxy node_modules/.bin/tsc --noEmit --ignoreConfig --strict --types node --target ES2022 --module NodeNext --moduleResolution NodeNext --skipLibCheck test/phase14c-contracts.test.ts` -> exit 0.
- GREEN: targeted eslint and `rtk proxy git diff --check` -> exit 0.
- Commit scope remains limited to the three changed Task 0.1 files; `framework-query.types.ts` and all pipeline/storage/package/lockfile files remain untouched.
Task 0.1: fix round 2/5 (3 remaining findings addressed; commits 7477eb0..88b7b63).
Task 0.1: complete (commits 7477eb0..88b7b63, review clean). Note: reviewer reported one environment-only Node typings mismatch during adversarial recheck; implementer had already passed production tsc.

Task 0.2 execution (2026-09-10):
- RED reproduced from the supplied partial diff: `node --import tsx/esm --test test/phase14c-objective-facts.test.ts` reported 6 tests, 4 passed and 2 failed. Kotlin failed `JVM annotations retain ownership, named arguments, parameter links, and Bean return types` because the Kotlin grammar places `@Qualifier` in sibling `parameter_modifiers`, so linking only the `parameter` node cannot back-link the annotation to its `parameter:*` FactLocalId. Dart failed `Dart materializes named widget arguments, generic provider types, and build returns` because `function_signature` and `function_body` are separate siblings; the callable stack was popped after the signature, so collector-created/visited return observations inherited the enclosing class symbol instead of `build`.
- RED reproduced from the supplied partial diff: `node_modules/.bin/tsc --noEmit` reported `facts-codec.ts` lines 359, 360, 362 implicit-any callback parameters and line 400 `Object` of type `unknown`. Root cause was loss of narrowing from dynamic record fields and an untyped `value.nodes` array in `hasObjectiveSyntax`.
- AST/pattern evidence: Java `@Qualifier` is nested under `formal_parameter`, and its existing `syntax.linkFact(parameter, parameterId)` path already works; Kotlin requires linking annotation descendants of the immediately preceding `parameter_modifiers`. Dart `function_body` ownership is carried only for that subtree by temporarily pushing `pendingBodyOwner` onto `callableStack`; no dynamic framework meaning is inferred.
- Fixes: link Kotlin parameter annotations to the created parameter fact; carry Dart function-body ownership through return/lambda traversal; narrow codec nodes to `SyntaxObservation[]` and validate fact arrays before collecting local IDs; update the Phase 14B contract fixture assertions for the additive objective syntax while asserting preserved `expression:3` IDs.
- GREEN: `node --import tsx/esm --test test/phase14b-facts-contract.test.ts` -> 4 tests, 4 passed, 0 failed.
- GREEN: `node --import tsx/esm --test test/phase14c-objective-facts.test.ts` -> 6 tests, 6 passed, 0 failed.
- GREEN: `node_modules/.bin/tsc --noEmit` -> exit 0.
- GREEN: `git diff --check` -> exit 0.
- GREEN: targeted `node_modules/.bin/eslint` over the 8 tracked Task 0.2 source/test paths plus the two created Task 0.2 paths -> exit 0.
- Scope check: `git status --short` lists only the 8 approved tracked paths, `src/core/facts/objective-syntax.types.ts`, `test/phase14c-objective-facts.test.ts`, and this required progress ledger.
- `gitnexus detect_changes --scope all --repo <HOME>/code-atlas` completed with the required stale-index warning: the index was built at `<HOME>/code-atlas`, the current worktree is a sibling clone 150 commits ahead, and results may be stale. It reported 8 files, 1 symbol (`vectorRefreshMode`), 0 affected processes, low risk. Running without `--repo` was blocked by multiple registered repositories; reindexing was not run because the task requires the existing partial worktree/index evidence to remain intact.
Ruling: Task 0.2 may modify test/phase14b-facts-cache.test.ts narrowly because the sole approved factsVersion bump necessarily changes its compatibility fixture; no production scope or second version owner is introduced.

Task 0.2 fix round 1 report (2026-09-10):
- Ruling reference: the controller-authorized compatibility exception above permits only `test/phase14b-facts-cache.test.ts` to move its current fixture to facts 3.0.0 while retaining a facts 2.0.0 old-blob miss.
- Root causes: ECMAScript declarations registered owners after decorators were visited without a persistent declaration link; Dart selectors were classified with an uppercase semantic guess instead of their parser-proven member/argument shape; property fallback resolved a missing value to the observation itself; objective codec validation checked link existence but not cycles or empty/partial-success semantics.
- RED: baseline `rtk proxy node --import tsx/esm --test test/phase14b-facts-cache.test.ts` -> 1 test, 0 passed, 1 failed (`3.0.0 !== 2.0.0`). Added regressions then ran objective/cache tests -> 11 tests, 7 passed, 4 failed (cycle acceptance, missing decorator owner, shorthand-property self-link, and Dart selector distinction).
- Fixes: persist declaration ownership for class and method decorators; classify Dart member selectors as properties and only argument selectors as calls, reserving construct for parser-proven construction; remove self child/argument links; reject cyclic, empty, and incomplete-without-unknown objective syntax; preserve deterministic syntax IDs, one parse, existing FactLocalId allocation, source side-channel prohibition, and Nest/Flutter/JVM coverage.
- GREEN: focused objective/facts/cache/extractor tests -> 19 passed, 0 failed, 0 skipped; `rtk proxy node_modules/.bin/tsc --noEmit`, targeted eslint over the five changed TypeScript files, and `rtk git diff --check` -> exit 0.
- GitNexus pre-commit `detect_changes --scope all --repo <HOME>/code-atlas` emitted the required stale warning (sibling clone 151 commits ahead) and reported no changes; this result is not used as proof of zero impact. Direct diff review confirms only the three Task 0.2 implementation files, two authorized tests, and this ledger changed; no package, lockfile, pipeline, storage, framework adapter, or version-owner file changed.

Task 0.2 fix round 2 report (2026-09-10):
- Reviewer probe reproduced before the fix: `@Controller("users") export class C {}` emitted a `Controller` annotation with no `ownerSymbolId`; the existing non-exported class and inner method owner test did not cover this sibling shape.
- Root cause: ECMAScript syntax observation runs before the class symbol is created, and decorator back-linking covered `method_definition` only; in the exported AST the decorator is a preceding sibling of `class_declaration` under `export_statement`.
- TDD RED: `node --import tsx/esm --test test/phase14c-objective-facts.test.ts` -> 10 tests, 9 passed, 1 failed (`Controller` owner assertion) after changing the regression fixture to `@Controller("users") export class Users { @Get() list() {} }`.
- Fix: standard exported class decorators now back-link through the created class symbol using the same immediate-preceding-decorator rule already used for methods; inner method ownership remains covered.
- TDD GREEN: `node --import tsx/esm --test test/phase14c-objective-facts.test.ts` -> 10 tests, 10 passed, 0 failed, 0 skipped.
- Reviewer probe GREEN: `@Controller("users") export class C {}` -> `annotationOwnerSymbolId: symbol:7`, `ownerName: C`.
- Focused GREEN: `node --import tsx/esm --test test/phase14c-objective-facts.test.ts test/phase14b-facts-cache.test.ts test/phase14b-fact-extractor-dispatch.test.ts` -> 15 tests, 15 passed, 0 failed, 0 skipped.
- GREEN: `node_modules/.bin/tsc --noEmit` -> exit 0; targeted ESLint over the five Task 0.2 commit paths -> exit 0; `git diff --check` -> exit 0.
- GitNexus `impact` and `detect_changes` used the indexed `<HOME>/code-atlas` repository and emitted the required stale sibling-clone warning; the edited extractor symbols were absent from that stale index (`UNKNOWN` impact), and `detect_changes` reported no changes. These results are not treated as proof of zero impact; no reindex was run.
- Amended commit: `e350bcb33df4a806aed36d54481b24f2a7f7aace1`; `git show --name-only` lists exactly `src/core/facts/extractors/ecmascript.ts`, `src/core/facts/facts-codec.ts`, `src/core/facts/objective-syntax.types.ts`, `test/phase14b-facts-cache.test.ts`, and `test/phase14c-objective-facts.test.ts`. No package/lockfile paths are included.
- The current ledger content remains on disk outside the amended commit and is removed from the Git index after this report; because the commit parent tracks the prior ledger blob, Git may display the path as an intent-to-remove working-tree entry rather than `??` without rewriting the parent commit.
- Review status: no P0/P1/P2 findings remain from the listed reviewer probe/scope; no merge, push, publish, or worktree deletion performed.
Task 0.2: fix round 2/5 (exported owner and commit scope addressed; amended commit 590f900 -> e350bcb).
Task 0.2: complete (commits 069a257..e350bcb, review clean; 15/15 focused).

Task 0.3 execution (2026-09-10):
- Added the sole `FRAMEWORK_RESOLUTION_VERSION = "1.0.0"` owner; current version domains persist it while the type accepts legacy absence.
- Framework-only version changes and missing legacy framework versions preserve parsed facts and language resolution paths (`parsePaths=[]`, `resolvePaths=[]`, `fullGraphResolution=false`); language resolution changes still resolve without parsing.
- `factsInvalidated` now compares `factsSchemaVersion` and `factsVersion`; persistence `schemaVersion` changes alone do not reparse facts.
- RED: `rtk proxy node --import tsx/esm --test test/phase14c-version-domains.test.ts` failed with the missing `FRAMEWORK_RESOLUTION_VERSION` export (exit 1).
- GREEN: focused Phase 14C/14A/14B version-invalidation suite -> 14 tests, 14 passed, 0 failed, 0 skipped.
- GREEN: `rtk proxy node_modules/.bin/tsc --noEmit`, targeted ESLint, and `rtk proxy git diff --check` -> exit 0.
- Commit: `b6c31d73c7ca8f4b8df937ff09e14e20850f65e2` (`feat(framework): separate framework resolution version`); commit contains exactly the three production files and `test/phase14c-version-domains.test.ts`.
- The Phase 14B key-list fixture has a separate unstaged one-line compatibility update so the requested combined suite reflects the new enumerable current domain; the required ledger remains unstaged.
- GitNexus `detect_changes --scope all --repo <HOME>/code-atlas` emitted the stale sibling-clone warning and reported no changes; result was not treated as zero-impact proof.

Task 0.3 execution (2026-09-10):
- TDD RED: `rtk proxy node --import tsx/esm --test test/phase14c-version-domains.test.ts` -> module import failed because `FRAMEWORK_RESOLUTION_VERSION` was not exported before implementation.
- GREEN focused: `rtk proxy node --import tsx/esm --test test/phase14c-version-domains.test.ts test/phase14a-invalidation.test.ts test/phase14b-version-domains.test.ts` -> 14 tests, 14 passed, 0 failed, 0 skipped.
- GREEN: `rtk proxy node_modules/.bin/tsc --noEmit` -> exit 0.
- GREEN: targeted `rtk proxy node_modules/.bin/eslint src/core/repository/index-version.ts src/core/facts/facts.types.ts src/core/indexing/invalidation-planner.ts test/phase14c-version-domains.test.ts` -> exit 0.
- GREEN: `rtk proxy git diff --cached --check` -> exit 0.
- GitNexus impact before edit used the indexed `<HOME>/code-atlas` repository and returned UNKNOWN for the worktree symbols; it emitted the stale sibling-clone warning (worktree 152 commits ahead), so the result was not treated as zero-impact proof.
- GitNexus `detect-changes --scope staged --repo <HOME>/code-atlas` emitted the same stale warning and reported 4 files, 1 indexed symbol (`vectorRefreshMode`), 0 affected processes, low risk; stale-index limitation remains explicit.
- Commit: `b6c31d73c7ca8f4b8df937ff09e14e20850f65e2` (`feat(framework): separate framework resolution version`), parent `e350bcb33df4a806aed8ef32e8b41458e8ab046d`, 4 files, 117 insertions, 1 deletion.
- Version-domain ownership: `FRAMEWORK_RESOLUTION_VERSION = 1.0.0` is the sole framework semantic owner; `IndexVersionDomains.frameworkResolutionVersion` is optional for legacy manifests and present in current domains. `factsSchemaVersion`/`factsVersion`, `parserIdentity`, Phase 14B `resolutionVersion`, and `derivedVersion` remain unchanged. `factsInvalidated` now compares facts schema/version, not persistence `schemaVersion`; framework-only and legacy-missing changes remain parse/resolve-free.
- Exact committed paths: `src/core/facts/facts.types.ts`, `src/core/indexing/invalidation-planner.ts`, `src/core/repository/index-version.ts`, `test/phase14c-version-domains.test.ts`. The required execution ledger, `test/phase14b-version-domains.test.ts` user change, package files, and lockfiles were not staged or committed.
- Task 0.3 review ruling: framework-only rebuild signaling/materialization is intentionally deferred to Task 1.4, which owns framework dependency closure; Task 0.3 asserts only language parse/resolve-free version-domain behavior.
- Task 0.3 fix: committed the Phase 14B version-domain compatibility fixture update in amended commit `4acfdfab5483651ae6b8769868fd942bfcebaf85`; no P0/P1/P2 findings remain.
- Task 0.3: complete (commit `4acfdfa`, review approved; 14/14 focused).
- Task 0.4 review ruling: `test/phase14b-provenance-storage.test.ts` is an approved narrow compatibility exception because the sole schema-version bump necessarily changes its expected schema value; no production scope drift.
- Task 0.4: complete (commits `f67a675`, `5e29061`, `15631c5`, `e4275ca`; atomic migration/read-only/rollback review clean; focused storage regressions previously 17/17).

Task 0.3 review fix (2026-09-10):
- P1 reproduced from clean commit: `test/phase14b-version-domains.test.ts` expected the pre-14C five-key enumerable domain list while current domains correctly expose `frameworkResolutionVersion`.
- TDD RED: temporarily removing the expected key produced 1 failed test with the actual extra `frameworkResolutionVersion` assertion diff.
- Fix: added `frameworkResolutionVersion` to the Phase 14B compatibility fixture only; no production behavior changed.
- GREEN: focused Phase 14C/14A/14B version-invalidation suite -> 14 tests, 14 passed, 0 failed, 0 skipped; typecheck, targeted ESLint, and diff check passed.
- Amended commit: `4acfdfab5483651ae6b8769868fd942bfcebaf85` (`feat(framework): separate framework resolution version`), five committed files including the compatibility fixture.
- Boundary preserved: framework rebuild signaling is explicitly deferred to later Task 1.4 pipeline/invalidation closure; Task 0.3 does not add it.
- GitNexus staged detect emitted the stale sibling-clone warning and reported no changes; result was not treated as zero-impact proof. This ledger remains unstaged.

Task 0.4 execution (2026-09-10):
- Implemented `ATLAS_SCHEMA_VERSION = "3"` and `INDEX_SCHEMA_VERSION = "2.0.0"`; the latter remains the sole persistence/schema version owner. `factsVersion`, facts schema metadata, parser identity, language resolution version, and derived version semantics were not changed.
- Added generation-scoped framework entity, relationship, classification, diagnostic, coverage, and state tables; canonical entity tuple uniqueness; generation indexes/foreign keys; and nullable `active_framework_resolution_version` metadata. No framework rows were added to `generation_symbols`.
- Refactored schema creation so schema-1/schema-2 writable upgrades perform atomic legacy repair plus 1->2->3 / 2->3 migration; schema-1/schema-2/schema-3 read-only validation remains non-mutating. Legacy active framework materialization remains null/unavailable until a later writable publish.
- TDD RED: `rtk proxy node --import tsx/esm --test test/phase14c-schema.test.ts` -> 7 tests, 2 passed, 5 failed on missing schema-3/framework behavior.
- GREEN: `rtk proxy node --import tsx/esm --test test/phase14c-schema.test.ts test/phase14b-readonly-migration.test.ts` -> 8 tests, 8 passed, 0 failed, 0 skipped.
- GREEN: `rtk proxy node_modules/.bin/tsc --noEmit`, targeted ESLint for the three Task 0.4 paths, and `rtk proxy git diff --check` -> exit 0.
- GitNexus `detect-changes --scope staged --repo <HOME>/code-atlas` reported 3 files, 2 indexed symbols, 0 affected processes, low risk, with the required stale sibling-clone warning; the result is not treated as zero-impact proof and no reindex was run.
- Commit: `f67a675` (`feat(storage): add generation framework schema`), exactly the three Task 0.4 paths. This ledger entry remains unstaged.
- Ruling: Task 0.4 owns persistence/schema DDL and migration only; typed framework materialization, publish, and active-generation availability remain deferred to Tasks 0.5/1.3.

Task 0.5: complete (commit cc5ff13, exact files `src/storage/atlas/atlas.store.ts`, `src/storage/atlas/atlas.types.ts`, `test/phase14c-storage.test.ts`; 850 insertions). Added atomic candidate framework materialization writes, typed deterministic reads, endpoint/provenance/classification validation, bounded sorted refs, rollback/rejection paths, legacy graph-only compatibility, and one-transaction query inputs. `git diff --check` passed and GitNexus staged detect reported low risk with 0 affected processes, but its sibling-worktree index was stale. Focused tests, repository tsc, and ESLint could not run to completion because the worktree has no hydrated package links; `pnpm exec tsc` attempted registry downloads and hit DNS/network `ENOTFOUND`, while direct fallback reported missing `tree-sitter`, `uuid`, and `node:sqlite` typings and ESLint reported missing `@eslint/js`. Ledger intentionally remains unstaged; publish integration remains deferred to 1.3.
- Controller verification after offline dependency hydration: Task 0.5 storage/regression bundle 13/13 passed, direct `tsc --noEmit` passed, targeted ESLint passed after adding an error cause, and `git diff --check` passed. Test-only reopen/diagnostic assertions were corrected in `3d71f5c`; the lint fix is `dcf487d`.
- Fresh subagent review is pending: the reviewer hit the gpt-reserve weekly limit (reset reported 2026-09-14 11:46 +07), so no review approval is claimed and later task dispatch is paused until review capacity returns or the user explicitly directs a controller-only fallback.
- Controller-only fallback authorized by user: continued implementation with main-session quota after the reserve bucket exhausted; subagent review remains unavailable.
- Task 1.1: complete (commit `7b3f431`; config tests 3/3, tsc/lint/diff pass). Pipeline now exposes deterministic materialized config inputs; native parser regression remains environment-blocked by missing `tree-sitter-kotlin` Darwin arm64 build.
- Task 1.2: complete (commit `fe41e48`; gate/contracts 14/14, tsc/lint/diff pass). Added deterministic route canonicalization, adapter registry ordering, typed unique-or-drop evidence gate, and classification/relationship diagnostics. Controller-reviewed; no subagent review available.
- Task 1.3: complete (commit `4666106`; publication + storage tests 7/7, tsc/lint/diff pass). Pipeline stages a complete framework snapshot before publication; versioned candidates require the marker and matching complete state; active framework resolution version is persisted. Controller-reviewed; no subagent review available.
- Task 1.4: complete (commit `cc3a453`; invalidation tests 4/4, tsc/lint/diff pass). Added bounded framework dependency invalidation and framework resolved/reused counters; pipeline uses framework scope independently of language parse/resolve scope. The broader Phase14A/14B pipeline bundle remains blocked by the pre-existing missing `tree-sitter-kotlin` Darwin arm64 native build. Controller-reviewed; no subagent review available.
- Task 1.5: committed in `c3d40b7` (`test(framework): add shared lifecycle conformance harness`) with real-pipeline fixture, deterministic normalization, clean rebuild, version-bump hook, and lifecycle tests; Phase14A exact counter expectations were updated for the two new framework counters. The conformance suite is environment-blocked before test loading by missing `tree-sitter-kotlin` Darwin arm64 native build; ESLint and diff-check pass. Controller-reviewed; no subagent review available.
- Task 2.1: complete (commit `0a11a35`; React/Next detection and identity tests 4/4, tsc/lint/diff pass). Added separately detected React/Next scopes and deterministic Next route canonicalization; adapter activation remains deferred to Task 2.4. Controller-reviewed; no subagent review available.
- Task 2.2: complete (commit `974210f`; component evidence tests 2/2, tsc/lint/diff pass). Added explicit JSX component-usage evidence with unique language targets and intrinsic-tag exclusion; registry activation remains deferred to Task 2.4. Controller-reviewed; no subagent review available.
- Task 2.3: complete (commit `c26ccc6`; Next route/classification tests 2/2, tsc/lint/diff pass). Added convention-based Next route entities/route bindings and typed `use client`/`use server` classification evidence without fake targets. Controller-reviewed; no subagent review available.
- Task 2.4: complete (`e1f9f31` plus required cycle fix `f355243`; React/Next focused matrix 9/9, tsc/lint/diff pass). Activated the adapter deterministically and removed the registry import cycle by making Next canonicalization self-contained. Controller-reviewed; no subagent review available.
- Task 3.1: complete (commit `1956b61`; Nest route/detection tests 2/2, tsc/lint/diff pass). Added scoped Nest detection and canonical controller-route identity; module visibility, endpoint evidence, and DI remain subsequent tasks. Controller-reviewed; no subagent review available.
- Task 1.4 follow-up: commit `176a259` fixes hashed framework-config invalidation by matching changed config paths against both prior and current materialized input keys; invalidation tests 4/4, tsc/lint/diff pass.
- Task 3.2: complete (commit `bc5b266`; Nest module ownership test 1/1, tsc/lint/diff pass). Added objective-syntax-linked module provider/controller/import/export evidence and dependency buckets; controller self-review only, review pending.
- Task 3.3: complete (commit `06b7e92`; Nest injection tests 2/2, tsc/lint/diff pass). Added unique typed constructor-provider relationships and ambiguity drop behavior; controller self-review only, review pending.
- Task 3.4: complete (commit `c23a8a7`; Nest focused matrix 6/6, tsc/lint/diff pass). Activated Nest alongside React/Next with plain-TypeScript isolation; controller self-review only, review pending.
- Task 4.1: complete (commit `c42bede`; Spring route/detection tests 2/2, tsc/lint/diff pass). Added Spring scoped detection and canonical route identity; beans/injection/activation remain subsequent tasks. Controller self-review only, review pending.
- Task 4.2: complete (commit `3cf4752`; Spring bean test 1/1, tsc/lint/diff pass). Added explicit configuration-owner to factory-method bean relationships with distinct endpoints; controller self-review only, review pending.
- Task 4.3: complete (commit `8185e5f`; Spring injection test 1/1, tsc/lint/diff pass). Added unique typed injection evidence; controller self-review only, review pending.
- Task 4.4: complete (commit `74d983d`; Spring/registry focused matrix 7/7, tsc/lint/diff pass). Activated Spring and updated cross-adapter registration expectations; controller self-review only, review pending.
- Task 5.1: complete (commit `e110111`; Flutter widget test 1/1, tsc/lint/diff pass). Added explicit widget-construction relationships from materialized Dart facts.
- Task 5.2: complete (commit `c33160b`; Flutter route/widget tests 2/2, tsc/lint/diff pass). Added stable named-route canonicalization.
- Task 5.3: complete (commit `9950c09`; Flutter provider test 1/1, tsc/lint/diff pass). Added explicit Provider generic target evidence.
- Task 5.4: complete (commit `7a5a803`; Flutter/cross-adapter conformance 7/7, tsc/lint/diff pass). Activated Flutter and updated cross-adapter registry expectations; controller self-review only, review pending.
- Task 6.1: complete (commit `33dd996`; query projection tests 2/2, tsc/lint/diff pass). Added typed framework projection and optional pinned-generation graph attachment while preserving language traversal fields.
- Task 6.2: complete (commit `e57fcbf`; coverage status tests 2/2, tsc/lint/diff pass). Added explicit framework coverage status semantics for missing/stale/incomplete snapshots; controller self-review only, review pending.
- Task 6.3: complete (commit `88b3289`; cross-framework/isolation/no-reparse/equivalence tests 3/3, tsc/lint/diff pass). Added contract-level lifecycle and contamination checks; controller self-review only, review pending.
- Task 6.4: complete (commit `4beaacb`; exact failure comparison 1/1, packed smoke opt-in 1 skip without path, tsc/lint/diff pass). Packed verification remains pending until a tarball path is supplied; controller self-review only, review pending.
- Fresh broad verification (2026-09-10): `node --import tsx/esm --test test/phase14c-*.test.ts` -> 79 total, 76 pass, 2 loader failures, 1 skip. Both failures are `tree-sitter-kotlin` Darwin arm64 native-build errors in `phase14c-core-conformance.test.ts` and `phase14c-objective-facts.test.ts`; no new assertion regression was observed. `pnpm run build`, `pnpm run lint`, and `pnpm run ui:typecheck` passed. `pnpm pack` passed; isolated npm tarball consumer install passed with temporary cache, but packed CLI help/unknown-command smoke was blocked by the same Kotlin native loader before dispatch. No closure document is claimed yet; broad review and final closure remain pending.
- Kotlin loader root-cause investigation (2026-09-10): standalone `import('tree-sitter-kotlin')` reproduces before CodeAtlas code with `Error: No native build was found for platform=darwin arch=arm64 runtime=node abi=127 uv=1 armv=8 libc=glibc node=22.23.2`, from `node-gyp-build` called by `tree-sitter-kotlin/bindings/node/index.js`. Environment: Node v22.23.2, Darwin arm64, pnpm 11.22.0, npm 10.9.8, `tree-sitter-kotlin` 0.3.8. The published package contains source and `binding.gyp`, but no `prebuilds/` or `.node` artifact; its install script is `node-gyp-build`, so a local build is required. By contrast, `tree-sitter-javascript` 0.25.0 loads successfully and ships `prebuilds/darwin-arm64/tree-sitter-javascript.node` (Mach-O arm64). The repository `pnpm-workspace.yaml` permits build scripts for tree-sitter and several grammars but not tree-sitter-kotlin; `node_modules/.modules.yaml` records `tree-sitter-kotlin@0.3.8(tree-sitter@0.25.1)` under `ignoredBuilds`. Root cause is therefore external install/package-manager state: a source-only Kotlin grammar has no prebuilt Darwin arm64 artifact and its required install build was suppressed by pnpm's allowBuilds policy. This is not a CodeAtlas loader bug and no framework semantic workaround was applied. Darwin Kotlin remains blocked pending an install with the package build explicitly permitted or a supported Linux x64 verification platform.
- Closure-gate recheck (2026-09-10): after restoring dependencies with `pnpm install --ignore-workspace --frozen-lockfile` (tracked workspace policy and lockfiles unchanged), direct `node --import tsx/esm --test test/phase14c-*.test.ts` again produced 79 total, 76 pass, 2 Kotlin loader failures, 1 skip. Direct build, lint, UI typecheck, Vite build, and `git diff --check` all passed. `npm pack` passed to a temporary directory. A temporary packed consumer could not be installed offline because pnpm metadata for `ignore@5.3.2` was absent; the network attempt did not reach a stable smoke result before timeout. Therefore packed CLI/MCP remain unverified in this turn, and no closure claim is made. Full direct test execution after an ignored-scripts reinstall was invalid (268 total, 201 pass, 66 fail) because native install scripts were intentionally disabled; those failures are not treated as product regressions. Independent reviewer sweep remains unavailable while the gpt-reserve base-model bucket is at 100% weekly usage; broad review is controller-only and closure remains pending.
- Kotlin local package-manager repair (2026-09-10): `pnpm ignored-builds` initially listed `tree-sitter-kotlin`; `pnpm approve-builds tree-sitter-kotlin` performed a targeted local approval/build only and temporarily added exactly `tree-sitter-kotlin: true` to `pnpm-workspace.yaml`. The build used Xcode CLT at `/Applications/Xcode.app/Contents/Developer`, Apple clang targeting `arm64-apple-darwin25.6.0`, Python 3.13.14 arm64, node-gyp 12.3.0, and Node 22.23.2 arm64; it produced `build/Release/tree_sitter_kotlin_binding.node` as Mach-O arm64. Standalone `import('tree-sitter-kotlin')` then passed. The temporary workspace line was removed; tracked workspace, package.json, and lockfiles are unchanged, while the ignored local native artifact remains usable in this worktree. Re-running the complete Phase14C suite now reaches a separate Dart loader blocker: `@driftlog/tree-sitter-dart@1.0.4` has no Darwin arm64 artifact and is still ignored by pnpm, so the suite remains `79 total, 76 pass, 2 loader-failure files, 1 skip`; Kotlin is no longer one of the failures. Packed consumer install with scripts disabled still fails before dispatch on its own missing Kotlin artifact; no packed approval/config was persisted. Phase14C closure remains pending and no Dart approval was performed because it is outside this request.
- Dart local package-manager repair (2026-09-10): standalone `import('@driftlog/tree-sitter-dart')` reproduced the same `No native build was found for platform=darwin arch=arm64 ... abi=127` error. `@driftlog/tree-sitter-dart@1.0.4` resolves under the pnpm virtual store, ships source/binding.gyp and `install: node-gyp-build`, has no `os`/`cpu` constraints, no `prebuilds/`, and no `.node` artifact; pnpm listed it in `ignored-builds`. `pnpm approve-builds '@driftlog/tree-sitter-dart'` temporarily added exactly `@driftlog/tree-sitter-dart: true`, built `build/Release/tree_sitter_dart_binding.node` as Mach-O arm64, and standalone Dart plus Kotlin imports passed. The temporary workspace line was removed. With both local native artifacts present, Phase14C passed `89 total, 88 pass, 0 fail, 1 skip`; Phase13/CLI regression passed `22/22`. Phase14B had `139 total, 136 pass, 2 assertion failures, 1 skip` in `filesystem mode detects changed module configuration files` and `incomplete semantic provenance forces repository resolution on unchanged sync`; these are CodeAtlas failures requiring separate investigation, not native-loader failures. Build, lint, TypeScript, UI typecheck, Vite build, and diff-check passed. Real packed npm install with lifecycle scripts enabled completed and produced arm64 Kotlin/Dart binaries; packed CLI `--help` passed, unknown command exited nonzero, and packed MCP initialize plus rejection smoke passed `4/4`. For clean pnpm installs, the exact persistent policy additions required are `@driftlog/tree-sitter-dart: true` and `tree-sitter-kotlin: true` under `allowBuilds`; neither was committed. Reviewer-pending blocker remains independent.
- Closure continuation (2026-09-10): clean fresh dependency hydration with the persistent two-entry `allowBuilds` policy built both Dart and Kotlin native bindings and standalone imports passed; pnpm still returned `ERR_PNPM_IGNORED_BUILDS` for unrelated C/C++/Go/Java/Python/Rust/Swift packages, so overall clean install remains environment/policy-blocked outside Phase14C scope. Committed policy-only change `1efa4f4` (`build: allow native grammar builds`) contains exactly `pnpm-workspace.yaml` with the two requested entries; package.json and lockfiles are unchanged.
- Phase14B investigation: on accepted baseline `ae335426`, `test/phase14b-pipeline-scope.test.ts` was `7/7`; at pre-fix Phase14C HEAD it was `5/7` with exactly `filesystem mode detects changed module configuration files` and `incomplete semantic provenance forces repository resolution on unchanged sync`. The first root cause was Phase14C strict config validation rejecting the valid empty root scope for `package.json`; the fix accepts a bounded string including `""`. The second was a Phase14B legacy fixture manually publishing a versioned candidate without the now-required complete framework materialization; the fixture now writes a valid empty snapshot and passes `frameworkStaged: true`, preserving the runtime invariant. Focused pipeline suite is now `7/7`; full Phase14B is `139 total, 138 pass, 0 fail, 1 skip`. Fix commit: `306285e` (`fix: preserve Phase 14C publication compatibility`), exactly `src/storage/atlas/atlas.store.ts` and `test/phase14b-pipeline-scope.test.ts`.
- Fresh verification after fixes: Phase14C `89 total, 88 pass, 0 fail, 1 skip`; Phase14B `139/138/0/1`; Phase13/CLI `22/22`; direct build, lint, TypeScript, UI typecheck, Vite build, and `git diff --check` passed. Full direct suite was `548 total, 516 pass, 30 fail, 2 skip`; failures are the known Phase14A fact-extraction/counter set and integration/config-environment set, plus Zoo stale-state expectation, with no new Phase14C failure. `npm pack` passed. A fresh packed consumer attempt with lifecycle scripts was inconclusive in the non-interactive wrapper; prior lifecycle-enabled packed verification remains `install pass`, packed CLI help/unknown pass, and packed MCP `4/4`; packed smoke must be rerun with a visible result before closure.
- Review state: all controller-only tasks remain `review pending`; no independent reviewer capacity was available. GitNexus detect-changes against the stale sibling index reported no changes but emitted the 188-commit-ahead warning, so it is not treated as zero-impact proof. Phase14C is not claimed complete; merge/push/publish/delete actions remain intentionally unperformed.
- Phase14C review remediation ledger (2026-09-10; all status pending until focused verification and independent re-review):
  - A1 dependencies: reviewers R1/R2; P1; plan 1.2/1.4 requires adapter dependencies, negative lookups, and persistence; owner `framework-registry.ts` → AtlasStore/pipeline; root cause `resolveFrameworkEvidence` discards adapter dependency results; minimal fix is deterministic aggregation and candidate persistence; verify dependency write/reload plus invalidation; re-review pending.
  - A2 analyzePaths: reviewer R2; P1; plan 1.4 requires adapters honor framework invalidation paths; owner adapters/registry/pipeline; root cause adapters iterate all facts and context reuse is not consumed; minimal fix is bounded path filtering plus compatible reuse composition; verify framework-only rebuild counters and no reparse; re-review pending.
  - A3 Next route/layout coverage: reviewer R2; P1; plan 2.3; owner React/Next adapter; root cause route convention handling is narrower than promised and scope/owner identity is hard-coded; minimal fix is objective-facts-only route/layout emission and collision-safe identity; verify route groups, pages/app/API, layouts, delete/rename; re-review pending.
  - A4 Nest endpoints/modules/DI: reviewers R1/R2; P1; plans 3.1–3.4; owner Nest adapter; root cause planned controller-route and cross-file visibility cases are not fully emitted/resolved; minimal fix is evidence-driven endpoint/module visibility and unique DI only; verify positive, ambiguity, delete/rename, query/storage/coverage; re-review pending.
  - A5 Spring endpoints/DI: reviewer R2; P1; plans 4.1/4.3; owner Spring adapter; root cause route evidence is canonicalizer-only and injection matching is too broad; minimal fix is annotation-owned route/constructor/field/method evidence with unique targets; verify Java/Kotlin positives and negatives; re-review pending.
  - A6 Flutter navigation: reviewer R2; P1; plan 5.2; owner Flutter adapter; root cause route canonicalization exists without navigation evidence; minimal fix is objective-facts-only static route/navigation relationships; verify named routes, explicit navigation, ambiguity/delete; re-review pending.
  - A7 adapter failure publication: reviewers R1/R3; P1; plan 1.2/1.3; owner registry/pipeline/storage; root cause adapter exceptions were returned as complete; fixed in current uncommitted review diff; verify throwing adapter and active-generation preservation; re-review pending.
  - B1 provenance merge: reviewers R1/R2; P1/P2; plan 1.2/1.5 and spec provenance; owner registry; root cause same identity evidence is first/last retained or treated as collision without deterministic merge; minimal fix is bounded sorted provenance merge and conflict rejection; verify duplicate/reordered/conflicting evidence; re-review pending.
  - B2 identity canonicalization: reviewer R1; P2; spec entity identity/canonical route rules; owner registry/adapters; root cause backslash normalization conflicts with persisted canonical validation; minimal fix is reject noncanonical input consistently; verify all route adapters; re-review pending.
  - B3 React imported target resolution: reviewer R2; P2; plan 2.2; owner React adapter/facts graph; root cause JSX target lookup is same-file display-name matching; minimal fix is resolved objective binding identity only; verify imported/member/ambiguous JSX and no guesses; re-review pending.
  - B4 budget accounting: reviewer R2; P2; plan 1.2; owner registry; root cause per-adapter slicing silently drops eligible evidence; minimal fix is deterministic global/per-adapter contract with budget diagnostics/coverage; verify overflow and no output leakage; re-review pending.
  - C1 coverage accounting: reviewer R3; P1; plan 6.2; owner registry/framework-coverage; root cause dimensions do not aggregate all observations or include output kind consistently; minimal fix is additive per-kind counters and conservative readiness; verify applicable/support/attempted/ambiguous/unsupported/budget; re-review pending.
  - C2 stale/incomplete propagation: reviewer R3; P1; plan 6.1/6.2; owner query/coverage/status; root cause projection readiness checks only config/detection; minimal fix is include version, complete flag, diagnostics and coverage uncertainty; verify stale/missing/incomplete snapshots and authoritative-negative=false; re-review pending.
  - C3 query consumers: reviewer R3; P1/P2; plan 6.1; owner indexed graph/explorer/impact/trace/inspect-change; root cause typed projection is not consistently surfaced through existing core consumers; minimal fix is additive framework attachment without changing symbol-only fields; verify query/retrieval and packed behavior; re-review pending.
  - C4 read-only/persistence gaps: reviewers R1/R3; P2; plans 0.5/6.2; owner AtlasStore/status/query; root cause read-only and typed snapshot paths lack complete byte/state assertions; minimal fix is preserve one pinned read and explicit incomplete state; verify DB/WAL/SHM invariance; re-review pending.
- Track A2 design (2026-09-11; implementation not started at design capture): ownership unit is each accepted evidence contribution, represented by its canonical provenance refs and `relativePath`; dependencies retain `ownerPath`. Semantic identity remains entity key, relationship source/target/kind, or classification subject/kind; candidate generation remains transport-only. The recompute set is direct `analyzePaths` plus all prior contributors of outputs intersecting it, so multi-source outputs are recomputed with remaining contributors rather than blindly copied. Reuse is limited to prior contributions whose owners do not intersect that set. Recomputed and reusable contributions are merged through one deterministic resolver; conflicting authoritative evidence produces ambiguity/conflict diagnostics, never first/last wins. Deleted paths are included as invalidation owners; their contribution disappears while surviving contributors are re-evaluated. A framework-resolution-version bump recomputes all current framework-applicable paths without parsing or language resolution. Diagnostics and coverage are contribution-scoped, then aggregated once to prevent double counting. Required tests: one-path adapter call bound, unchanged output reuse, output disappearance, multi-source survival, deletion, entity rename, dependency expansion, version-only rebuild, clean/incremental equivalence, and no duplicate coverage/diagnostics. This model uses existing provenance refs; no absolute paths or generation/order enter semantic identity.
- Independent review sweep (2026-09-10): three independent reviewers completed scoped reviews. No P0 findings. Reviewers independently identified genuine P1 contract gaps: framework adapter exceptions were marked complete and publishable (fixed in the uncommitted review-fix diff and covered by a new contract test); framework query traversal keys did not match `frameworkSubjectKey` and dropped queried nodes (fixed in the same diff and covered by a query assertion). They also identified remaining plan-level P1 gaps requiring more than closure-only edits: route/entity materialization coverage for planned Spring/Flutter/Nest/Next cases, dependency persistence and `analyzePaths` consumption, provenance merging, and complete framework projection/status exposure. These are confirmed against the canonical plans (1.2/1.4/2.3/4.1/5.2/6.1/6.2), not accepted as P3. No new framework feature work was started; closure remains blocked pending explicit scope to implement those planned gaps.
- Reviewer review status: tasks 0.5, 1.1-1.4, 2.1-2.4, 3.1-3.4, 4.1-4.4, 5.1-5.4, and 6.1-6.4 received independent review, but remain not-approved because P1 findings are unresolved. Reviewers also noted stale/missing GitNexus indexes; no graph result was treated as proof of safety. No merge, push, publish, branch deletion, or worktree deletion was performed.
- Track A remediation (2026-09-11): documented the ownership/recompute/reuse/merge model before coding. Added deterministic dependency aggregation and persistence, analyzePaths filtering with prior-output reuse, contributor expansion, deleted-path handling, dependency lookup expansion from materialized facts and prior graph nodes, provenance merge/conflict handling with legacy capability compatibility, capability-aware coverage aggregation, and diagnostic identity persistence. Focused Track A bundle: 41/41 pass; direct TypeScript, targeted ESLint, and diff-check pass. Independent reviewer re-reviewed A1+A2 after each remediation round and final status is approved: A1 resolved, A2 resolved, no P0/P1/P2/P3 findings. Track B/C and final closure verification remain intentionally unstarted.

Track B remediation map (2026-09-11; implementation begins only after this ledger entry):
- B1 provenance merge/conflict semantics — reviewers R1/R2; P1/P2; spec provenance and plans 0.4/1.2/1.5; affected registry, storage validation, and all adapters; root cause was scalar provenance retained across duplicate observations without an explicit compatible-union/conflict rule. Minimal behavior: sort/dedupe evidence IDs and refs, merge only compatible metadata, and drop conflicting authoritative outputs with deterministic conflict diagnostics. Focused tests: duplicate/reordered evidence, conflicting relationship/classification/entity evidence, contributor removal. Status: implemented in current Track A diff; independent Track B re-review pending.
- B2 FrameworkEntityRef identity — reviewer R1; P2; spec FrameworkEntityRef and canonical route identity; affected identity helper plus route adapters/store/query; root cause was inconsistent normalization versus persisted canonical validation. Minimal behavior: one tuple serializer/validator, reject noncanonical input, and keep framework entities separate from language symbols. Focused tests: all four route canonicalizers, stable rename/delete, malformed tuple rejection, query/store round trip. Status: current implementation present; independent re-review pending.
- B3 imported React/Next target resolution — reviewer R2; P2; plan 2.2; affected React adapter and language graph target lookup; root cause was same-file display-name matching. Minimal behavior: resolve objective JSX names against the full candidate language graph only when exactly one target exists; ambiguity stays unresolved. Focused test: imported unique target and duplicate-target ambiguity. Status: minimal fix present; independent re-review pending.
- B4 observation budget — reviewer R2; P2; plan 1.2; affected registry and coverage/diagnostics; root cause was adapter-local truncation with no global observable budget result. Minimal behavior: deterministic global bound, explicit budget-exhausted diagnostic, incomplete materialization, and conservative coverage. Focused test: multi-adapter overflow and no accepted output leakage. Status: minimal fix present; independent re-review pending.
- A3/Next semantic capability closure — reviewer R2; P1; plan 2.3; affected React/Next adapter through storage/query/coverage; root cause to verify: current convention handling does not yet prove pages/API handlers, route collisions, layout ownership, or objective-fact-only semantics end to end. Minimal behavior: emit only uniquely owned canonical route/layout entities and typed bindings; preserve collisions/ambiguity and delete/rename semantics. Focused/end-to-end tests: app/pages/API routes, route groups, nested layouts, collisions, config-incomplete prefixes, persistence/query/coverage.
- A4/Nest semantic capability closure — reviewers R1/R2; P1; plans 3.1–3.4; affected Nest adapter through storage/query/coverage; root cause to verify: current materialization does not yet prove controller endpoint entities, cross-file module visibility, or all bounded DI/token paths. Minimal behavior: objective-fact evidence only, unique endpoint/provider/module ownership, no guessed targets. Focused/end-to-end tests: controller routes, modules/imports/exports, cross-file unique/ambiguous DI, delete/rename, persistence/query/coverage.
- A5/Spring semantic capability closure — reviewer R2; P1; plans 4.1/4.3; affected Spring adapter through storage/query/coverage; root cause to verify: current route canonicalizer and constructor-only injection do not prove annotation-owned request mappings, field/method injection, or Java/Kotlin parity. Minimal behavior: annotation-owned route and injection evidence with unique targets and deterministic ambiguity. Focused/end-to-end tests: mappings, beans, constructor/field/method injection, Java/Kotlin positives and negatives, persistence/query/coverage.
- A6/Flutter navigation capability closure — reviewer R2; P1; plan 5.2; affected Flutter adapter through storage/query/coverage; root cause to verify: named-route canonicalization exists but navigation evidence is not materialized. Minimal behavior: objective-fact-only named-route declarations and explicit navigation relationships; unsupported dynamic navigation remains unresolved. Focused/end-to-end tests: named routes, explicit navigation, ambiguity/delete, persistence/query/coverage.
Track B grouping: B1/B2 share the typed output identity/provenance resolution boundary; B3/B4 are registry/adapter gate contracts; A3–A6 require per-framework end-to-end capability closure. Each finding requires reproduction, root-cause trace, focused and neighboring verification, and independent re-review before resolution. Track C remains unstarted.

Track B independent re-review (2026-09-11): reviewer Noether found no P0, but genuine P1 findings for Nest route ownership/path uncertainty, Spring mapping ownership/path uncertainty, React objective binding (current global name lookup remains a guess), relationship conflict poisoning, and a production A2 invalidation-path mismatch; P2 findings for budget coverage, unbounded provenance merge, route path canonicalization, and reused diagnostic completeness. All remain open pending root-cause fixes and re-review. Focused review snapshot: 52/52 pass; the passing happy paths do not cover these negative/determinism cases.

Track B local remediation round (2026-09-11): endpoint existence is now enforced against the materialized language/framework candidate universe; missing endpoints become unknown diagnostics and ambiguous candidates remain non-authoritative. Nest/Spring route and bean owners now require objective owner IDs plus graph containment; Nest/Spring injection owners no longer use line-range fallback and providers use the full graph with unique-or-drop resolution. React imported JSX targets require import-edge/qualified binding evidence. Conflict transitions decrement resolved coverage and increment ambiguous coverage; canonical route/entity validators reject separators, redundant segments, and dot segments; reused incomplete diagnostics/state remain incomplete; invalidation now distinguishes changed/deleted `paths` from the complete `allPaths` universe. Focused B1 suite: 50/50; Track A+B aggregate: 77/77; TypeScript, ESLint, and diff-check pass. Independent review pending; Track C remains unstarted.
Track B remediation round 2 (2026-09-11): locally reproduced and fixed the five remaining findings. Entity validation now runs after deterministic entity conflict elimination, so conflicted framework targets cannot authorize relationships. React imported JSX resolution follows objective graph file-import plus file-contains-symbol bindings, including aliases, with no global fallback. Cold classification conflicts now remove the prior resolved count. Next route extraction preserves repeated separators for canonical rejection; Flutter route identity preserves exact route names. Incremental completeness now tracks incomplete detection/dependency/diagnostic owners and permits recovery only when those owners are recomputed (or the whole universe is rebuilt). New focused regressions cover all five findings. Local status: resolved, independent review pending; Track C remains unstarted.
Track B remediation round 3 (2026-09-11): independent re-review found one remaining P2 in incremental coverage conflict aggregation. Added regressions for reused+recomputed relationship and classification conflicts, then changed conflict accounting to remove `resolved` from reused dimensions and assign `ambiguous` only to the recomputed conflict dimension. Local status: resolved after focused verification, independent re-review pending; Track C remains unstarted.
Track B final independent re-review (2026-09-11): reviewer Socrates rechecked internal recomputed relationship/classification conflicts, reused-output poisoning, exact ambiguity counting, and duplicate provenance coverage. Focused + neighboring checks: 29/29 pass; no P0/P1/P2/P3 findings. Track B approved. Track C remains intentionally unstarted by user scope.

Track C remediation map (2026-09-11; all items independent-review pending):
- C1 coverage accounting — reviewer R3 P1; spec §11 and plan 6.2; owner `framework-registry.ts` aggregation plus `framework-coverage.ts` readiness; invariant: applicable/supported/attempted/resolved/ambiguous/unknown/unsupported/budgetExhausted are per-output-kind, deterministic, nonnegative and mutually consistent, with conflicts excluded from resolved; focused regression: cold and incremental accepted→conflict, duplicate/reused evidence and budget overflow; status: audit/test pending.
- C2 configured/observed/supported distinction — spec §10/§11 and plan 6.2; owner adapter detection and coverage status; invariant: configured dependency presence does not imply observed constructs or full support, and observed unsupported constructs remain visible as unsupported/incomplete; focused regression: configured-only, observed-supported, observed-unsupported and zero-applicable snapshots; status: audit/test pending.
- C3 completeness propagation — reviewer R3 P1 and plans 1.2/1.3/6.2; owner registry materialization, framework status and query projection; invariant: adapter failure, budget exhaustion, relevant unknown/unsupported/config-incomplete evidence and reused incomplete contributions keep the candidate incomplete until owning evidence is successfully recomputed; focused regression: failure/budget/unknown, unrelated incremental change, and successful affected-path recovery; status: audit/test pending.
- C4 stale-state and authoritative-negative semantics — reviewer R3 P1/P2, spec §11/§21/§22 and plans 6.1/6.2; owner reuse merge, `framework-coverage.ts`, query/status consumers; invariant: stale diagnostics and outputs disappear when their owners are recomputed away, incomplete absence is never authoritative, and missing/stale/incomplete states remain distinguishable; focused regression: diagnostic resolution, deleted/renamed output, missing snapshot, stale version, and incomplete query/status projection; status: audit/test pending.
- C5 incremental coverage equivalence — spec §18/§25 and plan 6.3; owner registry reuse/invalidation and coverage aggregation; invariant: reused plus recomputed contributions are counted once, deletion/rename and dependency-expanded paths update only affected ownership, and normalized incremental coverage equals clean rebuild; focused regression: multi-source removal, dependency expansion, version-only rebuild, and clean/incremental normalized snapshots; status: audit/test pending.
- C6 diagnostic determinism and lifecycle — spec §12 and plan 1.2/6.2; owner registry diagnostics plus storage/query serialization; invariant: all required stable codes (including entity collision, subject ambiguity and classification conflict) are deterministic, deduplicated, persisted, and disappear only when the underlying issue is resolved; focused regression: every code, reordered evidence, reused/recomputed merge and resolution transition; status: audit/test pending.
- C7 read-only uncertainty exposure — reviewer R1/R3 P2, spec §22 and plan 0.5/6.2; owner AtlasStore read path, framework status/query projection and repository status; invariant: read-only calls do not mutate storage and expose `mayBeIncomplete`/`authoritativeNegativeResults` conservatively to downstream consumers; focused regression: byte/mtime/WAL/SHM invariance and incomplete query/status behavior; status: audit/test pending.

Track C execution rule: reproduce each item with a focused RED test, implement only the owning-layer fix, run neighboring tests, update this ledger, and obtain independent re-review before marking resolved. Do not create the closure record or proceed to broad/final verification until C is independently approved.

Track C local remediation round (2026-09-11; independent review pending):
- C1/C2: RED proved configured-only detection and applicable-but-unresolved coverage were reported as authoritative; status aggregation now treats configured-without-observation and unresolved applicable rows as incomplete. Resolver RED also proved exact `unsupported`/`supported:false` evidence could be accepted; unsupported/unknown evidence now yields stable diagnostics, coverage, and `complete:false` without accepted output. Focused coverage/resolver/contracts: 46/46 pass.
- C3/C4/C6: RED proved cold diagnostic materialization returned `complete:true`; resolver now derives completeness from diagnostics. Query RED proved incomplete/uncertain snapshot state was projected as complete; query projection now propagates materialization, detection, config, diagnostic, and coverage uncertainty. Lifecycle regression proves recomputed diagnostic owners clear stale diagnostics and recover completeness. Status: locally resolved, independent review pending.
- C5: Existing Track A/B reuse matrix covers unchanged reuse, deletion/rename, dependency expansion, version-only recomputation, clean/incremental normalization, duplicate coverage, and conflict transitions; rerun in Track C aggregate. Status: locally covered, independent review pending.
- C7: Existing storage/read-only tests remain unchanged and cover pinned-generation/read-only behavior; no new mutation was introduced. Status: locally covered, independent review pending.

Track C reviewer gate (2026-09-11): independent reviewer dispatch errored with the account usage limit before producing findings. C1-C7 remain `independent review pending`; no approval is claimed. Broad whole-branch review and final closure verification are blocked by this gate.

Track C independent review remediation (2026-09-11): reviewer Carver identified P1/P2 gaps in coverage-state validation, configured/observed readiness, cold and reused completeness propagation, pinned graph/query consistency, stale empty-reference diagnostics, subject/collision diagnostics, normalized clean/incremental comparison, and read-only byte/sidecar evidence. Local fixes add conservative coverage/readiness rules, storage counter validation, dependency/detection completeness propagation, deterministic diagnostic retention/deduplication, subject/collision diagnostics and coverage, one pinned graph/query input, full normalized incremental comparison, and a typed read-only DB/WAL/SHM invariance regression. Focused Track C remediation now passes 48/48; TypeScript, targeted ESLint, and diff-check pass. C1-C7 remain `independent review pending`; no local/controller review is treated as approval.

Track C independent review remediation round 2 (2026-09-11): reviewer Sartre found five remaining gaps: non-applicable evidence could retain supported/attempted counters and entity collision coverage was asymmetric (C1); detection readiness matched only framework, not capability (C2); repository status used stored rather than current framework version and semantic empty-reference diagnostics could survive adapter reruns (C4/C6); and clean/incremental equivalence lacked stable language-subject normalization (C5). Local fixes zero non-applicable counters, aggregate collision ambiguity once per semantic entity, require observed capability coverage, compare against `CURRENT_INDEX_VERSION_DOMAINS`, clear ownerless diagnostics when their adapter reruns, emit incomplete-config diagnostics, expose stale-version uncertainty through query/indexed graph/status, and normalize language subjects by objective node identity in the conformance helper. Focused remediation passes 53/53; independent approval remains pending.
Track C inline continuation (2026-09-11): user explicitly requested inline continuation without further subagents. Local review found one remaining capability-level readiness gap: an observed framework could be considered complete when coverage existed only for a different capability. `hasIncompleteDetectionInputs` now requires every observed capability to have matching applicable coverage; regression added in `phase14c-framework-reuse.test.ts`. Focused Track C/neighboring checks pass 55/55; full Phase14C passes 145 total, 144 pass, 0 fail, 1 skip; direct TypeScript, direct ESLint, and diff-check pass. This is local remediation evidence only; independent reviewer approval is not claimed. Package/lockfiles remain unchanged. Broad whole-branch review and final closure verification remain pending.
Phase14A delta investigation (2026-09-11): the current 11-name Phase14A failure set differs from the accepted nine-name waiver by two failures: `incremental graph matches a clean full rebuild for imports, calls, extends, malformed, delete, and rename` and `malformed source publishes reusable deterministic partial facts`. Each was reproduced individually at the pre-Phase14C baseline `a5bf0f8` and at current HEAD with the same assertion outcome (`failed` where the test expects `published`). Classification is inherited historical failure omitted from the old waiver, not a Phase14C regression and not order-dependent. Waiver expansion remains pending explicit approval; no automatic waiver change was made.
